#:schema https://jj-vcs.github.io/jj/latest/config-schema.json

[user]
name            = "musaubrian"
email           = "musaubrian15@gmail.com"

[ui]
default-command = "log"
diff-editor     = ":builtin"
merge-editor = ":builtin"

[signing]
behavior        = "own"
backend         = "ssh"
key            = "~/.ssh/signing.pub"

# [signing.backends.ssh]
# alowed-signers = "~/.ssh/allowed_signers"

[aliases]
difft           = ["diff", "--tool=difft"]
