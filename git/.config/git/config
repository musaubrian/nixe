[user]
	name=musaubrian
	email=musaubrian15@gmail.com

[init]
	defaultBranch=main

[core]
	editor=nvim

[alias]
	dl=-c diff.external=difft log --oneline -p --ext-diff # `git log` with patches shown with difftastic.
	dlc=-c diff.external=difft show --ext-diff # Show the most recent commit with difftastic.

	lg=log --oneline --graph --decorate
	rem=!git branch --set-upstream-to=origin/`git symbolic-ref --short HEAD`
	conflicts=!git ls-files -u | cut -f 2 | sort -u
	track-all="!git branch -r | grep -v '\\->' | while read remote; do local=${remote#origin/}; git show-ref --verify --quiet refs/heads/$local || git branch --track $local $remote; done"

[pull]
	rebase=true
[push]
	autoSetupRemote=true

[diff]
	external=difft
